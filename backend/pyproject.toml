[project]
name = "nexus"
version = "0.1.0"
description = "NEXUS AI Engineering Intelligence Platform Backend"
authors = [
    { name = "MUKULSHARMA2711", email = "mukul.kumar_cs.aiml24@gla.ac.in" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
nexus = "nexus:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
